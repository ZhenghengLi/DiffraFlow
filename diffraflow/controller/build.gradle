apply from: "$rootDir/gradle/common_cpp_app.gradle"

application {
    binaries.configureEach {
        linkTask.get().linkerArgs.add('-llog4cxx')
        linkTask.get().linkerArgs.add('-lzookeeper_mt')
    }
    dependencies {
        implementation project(":diffraflow:common")
    }
}

