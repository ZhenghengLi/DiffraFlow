#!/bin/bash
# debug
echo $SHELL
echo $COMMIT_MSG
COMMIT_MSG="${COMMIT_MSG%'\n'}"
echo $COMMIT_MSG
docker build --build-arg SOURCE_COMMIT="$SOURCE_COMMIT" --build-arg COMMIT_MSG="${COMMIT_MSG%'\n'}" -f $DOCKERFILE_PATH -t $IMAGE_NAME .
echo "done"
